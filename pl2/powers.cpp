#include <iostream>
#include <math.h>
#include <stdio.h>

using namespace std;

int main(){
	int i = 0;
	while(cout << "2 raised to the power " << i << " is " << pow(2,i) << endl, ++i, i < 11);


	return 0;
}
